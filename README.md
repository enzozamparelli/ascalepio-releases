# AscalepIO — Releases

Binari pubblici per auto-update di AscalepIO (updater Tauri).
La sorgente è privata; qui vivono solo gli artefatti firmati + `latest.json`.
Gestito automaticamente dalla CI (`release.yml`, job `publish-updater`).
